{
  "name": "relativity-warp",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-research.mjs",
    "post:new": "node scripts/new-post.mjs",
    "preview": "serve docs",
    "hooks:install": "git config core.hooksPath .githooks",
    "hooks:uninstall": "git config --unset core.hooksPath"
  },
  "devDependencies": {
    "marked": "^12.0.2",
    "serve": "^14.2.1"
  }
}
