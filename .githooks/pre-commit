#!/usr/bin/env bash
set -euo pipefail

if [ "${SKIP_PRECOMMIT_BUILD:-0}" = "1" ]; then
  echo "[pre-commit] SKIP_PRECOMMIT_BUILD=1, skipping local build"
  exit 0
fi

echo "[pre-commit] Running local build..."
npm run build

echo "[pre-commit] Staging generated docs..."
git add docs
